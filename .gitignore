# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js
app/.next/
app/out/
app/.monitor/

# Hardhat artifacts
contracts/cache/
contracts/artifacts/
contracts/ignition/deployments/
contracts/gas-stats.json
contracts/deployments/local.json

# Env / secrets
.env
.env.*
!.env.example
!**/.env.example

# Local agent session scaffolding, not part of the project
.claude/

# Superseded design comparison pages from the pre-Vigil skins. Ignored rather
# than deleted so they stay available locally; safe to remove when unwanted.
app/public/design-options/
